/* 页面背景 */
page {
  background-color: #f7f8fa; /* 浅灰底色，突显卡片 */
  padding-bottom: 50px;
}

.container {
  padding: 20px 0;
}

/* 标题样式 */
.title {
  text-align: center;
  font-size: 24px;
  font-weight: bold;
  color: #333;
  margin-bottom: 5px;
}

.subtitle {
  text-align: center;
  font-size: 14px;
  color: #999;
  margin-bottom: 20px;
}

/* 🌟 修复按钮样式：强制 100% 宽度 */
.btn-area {
  margin: 30px 16px;
  display: block;
  width: auto; /* 自动填满容器 */
}

/* 这是一个补充保险，强制 van-button 内部撑开 */
.van-button--block {
  width: 100% !important;
}

/* 结果卡片 */
.result-card {
  margin: 20px 16px;
  background: white;
  border-radius: 12px;
  padding: 20px;
  /* 柔和的阴影 */
  box-shadow: 0 8px 16px rgba(0,0,0,0.05);
  text-align: center;
  overflow: hidden; /* 防止子元素溢出圆角 */
}

.result-title {
  font-size: 14px;
  color: #666;
  margin-bottom: 8px;
}

.result-amount {
  font-size: 40px; /* 更大的数字 */
  font-weight: 800;
  color: #07c160; /* 微信标志绿 */
  margin-bottom: 20px;
  font-family: -apple-system, BlinkMacSystemFont, 'Helvetica Neue', Helvetica, sans-serif;
}