/* 页面背景 */
page {
  /* 🌟 使用更有质感的蓝灰色背景，增加对比 */
  background-color: #f0f2f5;
  padding-bottom: 50px;
  font-size: 16px;
}

/* 🌟 顶部品牌区域 */
.page-header {
  background: linear-gradient(180deg, #1989fa 0%, #0570e1 100%);
  padding: 40px 20px 60px;
  text-align: center;
  color: white;
}

.page-header .title {
  font-size: 30px;
  font-weight: 800;
  margin-bottom: 10px;
  letter-spacing: 1px;
}

.page-header .subtitle {
  font-size: 15px;
  opacity: 0.9;
  font-weight: 300;
}

/* 容器负外边距，让卡片嵌入页眉 */
.container {
  margin-top: -30px;
  padding: 0 0 20px;
}

/* 🌟 输入卡片增强 */
.input-card {
  box-shadow: 0 10px 25px rgba(0,0,0,0.08) !important;
  border-radius: 16px !important;
  overflow: hidden;
}

.van-cell, .van-field__input, .van-cell__title, .van-cell__value {
  font-size: 16px !important;
}

/* 按钮区域 */
.btn-area {
  margin: 35px 40px; /* 🌟 增加左右边距（16->40），让按钮变短，显得更精致 */
}

.van-button--primary {
  background: #1989fa !important;
  border: none !important;
  box-shadow: 0 8px 20px rgba(25, 137, 250, 0.2); /* 更淡更柔和的阴影 */
  border-radius: 10px !important; /* 稍微增加一点弧度 */
  /* 🌟 极简字重，增加上下空间 */
  height: 60px !important; 
  line-height: 60px !important;
  font-weight: 400 !important; 
  letter-spacing: 3px; /* 增加字间距，显得更舒展 */
}

.van-button--large {
  font-size: 19px !important; /* 稍微调小一点，比例更协调 */
}

/* 🌟 结果卡片视觉重构 */
.result-card {
  margin: 20px 16px;
  background: white;
  border-radius: 16px;
  padding: 30px 20px;
  box-shadow: 0 12px 30px rgba(25, 137, 250, 0.12); /* 蓝色投影 */
  text-align: center;
  position: relative;
  border: 1px solid rgba(25, 137, 250, 0.1);
}

.result-title {
  font-size: 16px;
  color: #666;
  margin-bottom: 15px;
  display: flex;
  align-items: center;
  justify-content: center;
}

.result-title::before, .result-title::after {
  content: "";
  width: 20px;
  height: 1px;
  background: #ddd;
  margin: 0 10px;
}

.result-amount {
  font-size: 52px;
  font-weight: 900;
  color: #1989fa; /* 绿色 -> 蓝色 */
  margin-bottom: 25px;
  text-shadow: 0 2px 10px rgba(25, 137, 250, 0.1);
}

/* 计算过程推导 */
.process-box {
  margin-top: 30px;
  text-align: left;
  background-color: #f8fbff; /* 浅蓝底色 */
  padding: 20px 18px;
  border-radius: 12px;
  border-left: 6px solid #1989fa;
}

.process-title {
  font-size: 18px;
  font-weight: bold;
  color: #1989fa;
  margin-bottom: 15px;
  display: flex;
  align-items: center;
}

.process-title::before {
  content: "📝";
  margin-right: 8px;
}

.process-p {
  font-size: 15px;
  color: #2c3e50;
  line-height: 1.8;
  margin-bottom: 15px;
}

/* 公式展示 */
.formula-box {
  margin-top: 25px;
  padding-top: 20px;
  border-top: 1px dashed #ced6e0;
}

.formula-title {
  font-size: 15px;
  font-weight: bold;
  color: #57606f;
  margin-bottom: 15px;
}

.formula-label {
  display: block;
  font-size: 13px;
  color: #747d8c;
  margin-bottom: 6px;
}

.formula-val {
  display: block;
  font-size: 13px;
  color: #2f3542;
  background: #f1f2f6;
  padding: 12px;
  border-radius: 8px;
  line-height: 1.6;
}

.feedback-area {
  margin-top: 40px;
  text-align: center;
}

.disclaimer {
  margin: 30px 20px 40px;
  text-align: center;
  color: #a4b0be;
  font-size: 13px;
  line-height: 1.8;
}
