/* 页面背景 */
page {
  background-color: #f7f8fa;
  padding-bottom: 80px;
}

.container {
  padding: 20px 0;
}

/* 标题样式 */
.title {
  text-align: center;
  font-size: 24px;
  font-weight: 800;
  color: #333;
  margin-bottom: 8px;
}

.subtitle {
  text-align: center;
  font-size: 13px;
  color: #999;
  margin-bottom: 24px;
}

/* 🌟 按钮区域修复：关键样式 */
.btn-area {
  margin: 30px 16px;
  /* 必须设置为 block 才能撑开宽度 */
  display: block;
  box-sizing: border-box;
}

/* 强制覆盖 Vant 按钮样式 */
.van-button--primary {
  width: 100% !important; /* 强制宽度 100% */
  box-shadow: 0 4px 12px rgba(7, 193, 96, 0.4) !important; /* 绿色投影 */
  font-weight: bold !important;
  font-size: 16px !important;
  border-radius: 999px !important; /* 确保是大圆角 */
}

/* 结果卡片 */
.result-card {
  margin: 20px 16px;
  background: white;
  border-radius: 16px;
  padding: 24px 20px;
  box-shadow: 0 8px 20px rgba(0,0,0,0.06);
  text-align: center;
}

.result-title {
  font-size: 14px;
  color: #888;
  margin-bottom: 12px;
}

.result-amount {
  font-size: 42px;
  font-weight: 800;
  color: #07c160;
  margin-bottom: 24px;
  font-family: -apple-system, BlinkMacSystemFont, 'Helvetica Neue', Helvetica, sans-serif;
}